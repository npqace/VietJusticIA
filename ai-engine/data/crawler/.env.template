# This is a template for your .env file.
# Copy this file to a new file named .env and fill in your authentication token.

# How to get your AUTH_TOKEN from Local Storage (Updated Instructions):
# 1. Log in to https://aitracuuluat.vn/
# 2. Open your browser's Developer Tools (usually by pressing F12).
# 3. In the Developer Tools window, click on the "Application" tab.
#    (Note: In Firefox, this tab is called "Storage").
# 4. On the left-hand side, expand the "Local Storage" section.
# 5. Click on the entry for "https://aitracuuluat.vn".
# 6. A table of key-value pairs will appear on the right. Look for a key named "access_token".
# 7. The value associated with "access_token" is your token. It will be a very long string.
# 8. Copy that entire long string.
# 9. Paste it into this file, making sure to format it like this: AUTH_TOKEN="Bearer [paste_the_long_string_here]"
#    (Ensure there is a space between "Bearer" and the token).

AUTH_TOKEN="Bearer ..."